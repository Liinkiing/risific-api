RisificOrderField:
  type: enum
  config:
    description: Properties by which risifics can be ordered
    values:
      CREATED_AT:
        value: createdAt
        description: Allows ordering a list of risifics by when they were created.
      CHAPTERS_COUNT:
        value: chaptersCount
        description: Allows ordering a list of risifics by their chapters count.